[workspace]

members = [
    "course-manager-cli",
    "course-manager"
]

[workspace.metadata.release]
shared-version = true
tag-name = "v{{version}}"

# generated by 'cargo dist init'
[profile.dist]
inherits = "release"
debug = true
split-debuginfo = "packed"

